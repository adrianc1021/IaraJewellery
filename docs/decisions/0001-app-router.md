# ADR 0001: Single Next.js Application

Status: accepted

The first deployable release uses one Next.js service for storefront, APIs, account and operations. This reduces operational overhead while retaining feature folders and strict server boundaries. Sanity Studio can be split into `apps/studio` after project credentials are supplied.

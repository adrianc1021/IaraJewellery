"use client";
import { FormEvent, useState } from "react";
export function PromotionForm() { const [message,setMessage]=useState(""); async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/ops/promotions",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...form,value:Number(form.value),minimumMinor:Number(form.minimumMinor)*100,usageLimit:Number(form.usageLimit)})});const data=await response.json();setMessage(response.ok?"優惠已建立。":data.error);if(response.ok)setTimeout(()=>location.reload(),400);} return <form className="ops-form" onSubmit={submit}><div className="field"><label htmlFor="promo-name">名稱</label><input id="promo-name" name="name" required /></div><div className="field"><label htmlFor="promo-code">優惠碼</label><input id="promo-code" name="code" required /></div><div className="field"><label htmlFor="promo-value">折扣百分比</label><input id="promo-value" name="value" type="number" min="1" max="100" required /></div><div className="field"><label htmlFor="promo-min">最低消費 HK$</label><input id="promo-min" name="minimumMinor" type="number" min="0" defaultValue="0" /></div><div className="field"><label htmlFor="promo-limit">使用上限</label><input id="promo-limit" name="usageLimit" type="number" min="1" defaultValue="100" /></div><div className="field"><label htmlFor="promo-start">開始日期</label><input id="promo-start" name="startsAt" type="date" required /></div><div className="field"><label htmlFor="promo-end">結束日期</label><input id="promo-end" name="endsAt" type="date" required /></div><button className="button button-primary">建立優惠</button>{message&&<span>{message}</span>}</form>; }
